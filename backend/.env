# Server Configuration
PORT=5001
NODE_ENV=development

# MongoDB Atlas Configuration
MONGODB_URI=mongodb+srv://gottigetlit:note@cluster0.ox6vzw9.mongodb.net/cluster0?appName=Cluster0
MONGODB_TEST_URI=mongodb+srv://gottigetlit:note@cluster0.ox6vzw9.mongodb.net/cluster0?appName=Cluster0

# CORS Configuration
# Comma-separated origins allowed to access the API (e.g. https://yello-uk.web.app)
FRONTEND_URL=https://yello-uk.web.app

# API Configuration
API_PREFIX=/api
VITE_API_BASE_URL=http://localhost:5001/api

# JWT / Cookie Secrets (set secure values in Render or your host)
JWT_SECRET=replace-with-secure-random-value
COOKIE_SECRET=replace-with-secure-random-value

# Optional: allow SameSite=None for cross-site cookies (only if you understand the security tradeoffs)
# USE_SAMESITE_NONE=false